# Grok session archive

Snapshot of the Grok Build session that created this independent
(unsynced) repository from https://github.com/Fuminides/ex-fuzzy
and moved it to https://github.com/HAISymbiosis/EBD-AI.

Live session files remain under `~/.grok/sessions/` so the running
session is not disrupted. This directory is a snapshot, not the live
session store.
